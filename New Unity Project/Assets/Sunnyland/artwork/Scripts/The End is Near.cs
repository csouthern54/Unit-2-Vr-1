using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TheEndisNear : MonoBehaviour
{
    int first = 10;
    Rigidbody2D Player = null;
    // Start is called before the first frame update
    void Start()
    {
        
        Debug.Log(first);
        Debug.Log(Player);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
